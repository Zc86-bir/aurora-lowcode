apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aurora.fullname" . }}-config
  labels:
    {{- include "aurora.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.configMap }}
  {{ $key }}: |
    {{- $value | nindent 4 }}
  {{- end }}
